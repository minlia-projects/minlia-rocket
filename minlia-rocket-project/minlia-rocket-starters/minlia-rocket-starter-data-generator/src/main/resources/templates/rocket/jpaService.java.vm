package ${package.Service};

import ${package.Entity}.${entity};
import com.minlia.rocket.data.jpa.abstraction.service.AbstractJpaService;
import ${cfg.bodyPackage}.${entity}QueryRequestBody;
import ${cfg.bodyPackage}.${entity}PageableQueryRequestBody;

/**
 * @description: $!{table.comment} Jpa 服务
 * @copyright: ${cfg.copyright} ${cfg.year}
 * @createTime: ${cfg.createTime}
 * @author: ${author}
 * @version: ${cfg.version}
 */
public interface ${entity}JpaService extends AbstractJpaService<${entity}, Long, ${entity}QueryRequestBody, ${entity}PageableQueryRequestBody> {

}